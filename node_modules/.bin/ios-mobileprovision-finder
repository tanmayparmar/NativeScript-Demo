#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ios-mobileprovision-finder/ios-mobileprovision-finder.js" "$@"
  ret=$?
else 
  node  "$basedir/../ios-mobileprovision-finder/ios-mobileprovision-finder.js" "$@"
  ret=$?
fi
exit $ret
